interface:
  display_name: "KylinSoulInjector"
  short_description: "Build and verify KDI-layered Unity code safely"
  default_prompt: "Use $kdi-kylin-soul-injector to implement this Unity feature with explicit KDI layers, scopes, ownership, teardown, and verification."
policy:
  allow_implicit_invocation: true
